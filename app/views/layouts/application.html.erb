<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Deck Peddler - Rent MTG Cards and Decks for Magic: the Gathering Tournaments!</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="description" content="">
  <%= stylesheet_link_tag  "application", "bootstrap", "bootstrap-responsive", "font", "font-awesome", "base.css", "blue.css"%>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
  <link href='http://fonts.googleapis.com/css?family=Titillium+Web' rel='stylesheet' type='text/css'>
  <link rel="shortcut icon" href="img/favicon.ico">
  <link rel="apple-touch-icon-precomposed" sizes="144x144" href="<%= asset_path('img/favicon.ico')%>">
  <link rel="apple-touch-icon-precomposed" sizes="114x114" href="<%= asset_path('img/favicon.ico')%>">
  <link rel="apple-touch-icon-precomposed" sizes="72x72" href="<%= asset_path('img/favicon.ico')%>">
  <link rel="apple-touch-icon-precomposed" href="<%= asset_path('img/favicon.ico')%>">
</head>
<body>
<div class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container"> <a class="btn btn-primary btn-dropnav" data-toggle="collapse" data-target=".nav-collapse"> <span class="icon-bar"></span> <span class="icon-bar"></span> <span class="icon-bar"></span> </a> <a class="brand" href="<%= index_path %>"><img src="<%= asset_path('img/homebutton.png')%>" alt="" border="0"></a>
      <div class="nav-collapse collapse">
        <ul class="nav pull-right">
          <li><a href="<%= index_path %>">Home</a></li>
          <li><a href="<%= rent_singles_path %>">Rent Singles</a></li>
          <li><a href="<%= process_path %>">The Process</a></li>
          <li><a href="<%= faq_path %>">FAQ</a></li>
          <% if user_signed_in? == false %>
          <li><a href="<%= user_session_path %>">Login</a></li>
        <% else %>
          <li><a href="<%= user_session_path %>">Profile</a></li>
        <% end %>
    <div id="user_nav">
      <% if user_signed_in? %>
         Signed in as <%= current_user.email %>. Not you?
         <%= link_to "Sign out", destroy_user_session_path, :method => :delete %>
      <% else %>
        <%= link_to "Sign up", new_user_registration_path %> or <%= link_to " Sign in", new_user_session_path %>
      <% end %>
    </div>
  </div>
</div>
        </ul>
      </div>
    </div>
  </div>
</div>
<%= yield %>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.8.2/jquery.min.js"></script>
<script>
  $(document).ready(function(){
    $(".js-video").fitVids();
      $("#totop").click(function () {
    $("body, html").animate({
      scrollTop: 0
      }, 300);
    return false;
    }); 
  });
  </script>
</body>
</html>
